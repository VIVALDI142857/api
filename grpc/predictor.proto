syntax = "proto3";
package predictor;

service PredictorService {
  rpc PredictJson (PredictJsonRequest) returns (PredictResponse);
  rpc PredictRow (Row) returns (PredictResponse);
}

message PredictJsonRequest {
  string data = 1;
}
